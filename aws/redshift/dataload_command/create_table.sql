CREATE TABLE IF NOT EXISTS staging.copy_operation_demo (
            job VARCHAR,
            company VARCHAR,
            ssn VARCHAR,
            residence VARCHAR,
            blood_group VARCHAR,
            username VARCHAR,
            name VARCHAR,
            sex VARCHAR,
            address VARCHAR,
            mail VARCHAR
);